/**
 * DE:
 * Enum um die Rollen eines Nutzers zu repräsentieren.
 * EN:
 * Enum to represent the roles of a user.
 */
export enum Role {
  admin = 0,
  organizer = 1,
  basicUser = 2
}

export default Role;
