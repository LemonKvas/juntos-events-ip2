/**
 * DE:
 * Interface für Badges die Nutzer erhalten können.
 * EN:
 * Interface for badges that users can receive.
 */
export interface Badge {
  badgeId: string;
  title: string;
  condition: string;
}
