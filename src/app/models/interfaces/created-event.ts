/**
 * DE:
 * Interface um ein erstelltes Event zu repräsentieren.
 * EN:
 * Interface to represent a created event.
 */
export interface CreatedEvent {
  eventId: string;
  publishStatus: boolean;
}
