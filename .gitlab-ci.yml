image: agileek/ionic-framework

stages:
  - deploy

cache:
  untracked: true
  key: "$CI_PROJECT_ID"
  paths:
    - node_modules/

build:
  tags:
    - alpine 

#build:
#  stage: build
#  before_script:
#    - npm install -g @angular/cli@latest
#    - cd Juntos/
#    - npm install
#  script: 
#    - ng build
#  artifacts:
#    expire_in: 1 day
#    paths:
#      - Juntos/www/
#  tags:
#    - npm
