

stages:
  [build, deploy]



build:
  stage: build
  before_script:
    - npm install -g n lts
    - npm install -g @angular/cli@latest
    - cd Juntos/
    - npm install
  script: 
    - ng build
  artifacts:
    expire_in: 1 day
    paths:
      - Juntos/www/
  tags:
    - npm
