stages:
  [dependencies]

install_dependencies:
  image: node:12-alpine
  stage: dependencies
  script:
    - npm install
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules